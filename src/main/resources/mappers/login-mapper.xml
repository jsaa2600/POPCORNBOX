<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.login-mapper">

<select id="login" parameterType="Map" resultType="java.lang.Integer">
	select count(id) from member where id=#{id} and pw=#{pw}
</select>

 <select id="getMember" parameterType="Map" resultType="com.popcornbox.myapp.member.dto.MemberDTO">
 	select id,pw,nickname,email,selectEmail,tel1,tel2,tel3,gender from member where id=#{id} and pw=#{pw}
 </select>

</mapper>